# Default values for grafana-dashboards.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# -- Label to assign to configmaps
labels:
  grafana_dashboard: "true"
